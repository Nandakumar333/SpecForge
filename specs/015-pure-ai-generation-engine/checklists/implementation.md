# Implementation Checklist: Pure AI Content Generation Engine

**Purpose**: Validate that the implementation satisfies all 6 critical requirement areas for the Jinja2→LLM transition
**Created**: 2026-03-19
**Feature**: [specs/015-pure-ai-generation-engine/spec.md](../spec.md)

## 1. All 7 Phases Use Selected AI Instead of Jinja2

- [x] CHK-001 Are requirements clear that ALL 7 pipeline phases (spec, research, data-model, edge-cases, plan, checklist, tasks) call the LLM via `LLMProvider` instead of `TemplateRenderer.render()`? [Completeness, Spec §FR-021]
- [x] CHK-002 Is the dual-mode execution path in `BasePhase.run()` specified with an explicit mode flag selecting between template rendering and LLM generation? [Clarity, Spec §FR-022]
- [x] CHK-003 Is the LLM execution flow explicitly defined as `_build_prompt()` → `provider.call()` → `_validate_output()` → `_write_artifact()` for every phase? [Completeness, Spec §FR-023]
- [x] CHK-004 Does each of the 7 phase subclasses define its own `_build_prompt()` implementation returning phase-specific prompt context? [Completeness, Spec §FR-024]
- [x] CHK-005 Is LLM mode specified as the default when a supported agent is configured in `config.json`? [Clarity, Spec §FR-035]
- [x] CHK-006 Is template mode specified as the default when no agent is configured or agent is `"generic"`? [Clarity, Spec §FR-035]
- [x] CHK-007 Is there a success criterion that verifies 100% of pipeline artifacts are generated by the AI model with zero Jinja2 rendering in the content path? [Measurability, Spec §SC-001]
- [x] CHK-008 Is the `PipelineOrchestrator` specified to accept optional `LLMProvider`, `PromptAssembler`, `OutputValidator`, and `OutputPostprocessor` dependencies? [Completeness, Plan §Phase 4]
- [x] CHK-009 Is the parallel execution of data-model and edge-case phases explicitly preserved in LLM mode? [Completeness, Spec §FR-025]
- [x] CHK-010 Are requirements clear that each PhasePrompt embeds the exact Spec-Kit template skeleton as the target output format? [Clarity, Spec §FR-024, FR-045]

## 2. Decompose Uses Real LLM for Feature Discovery

- [x] CHK-011 Is it specified that `specforge decompose` constructs a prompt containing the app description, architecture type, constitution, and governance rules? [Completeness, Spec §FR-017]
- [x] CHK-012 Is the LLM response format for decompose defined as structured JSON (or clearly delimited sections) that can be reliably parsed? [Clarity, Spec §FR-019]
- [x] CHK-013 Is it specified that the decompose parser handles minor formatting deviations (extra whitespace, trailing commas in JSON)? [Coverage, Spec §FR-019]
- [x] CHK-014 Is the decompose output mapped to the same data structures used by `ManifestWriter` (features list, service mappings, dependencies, architecture type)? [Completeness, Spec §FR-018]
- [x] CHK-015 Is the fallback behavior defined: retry once with corrective prompt, then fall back to `DomainAnalyzer` if retry also fails? [Completeness, Spec §FR-020]
- [x] CHK-016 Is the success criterion that LLM decompose produces a valid `manifest.json` with at least 3 distinct features measurable and testable? [Measurability, Spec §SC-006]
- [x] CHK-017 Are architecture-specific decomposition instructions defined for both `--arch microservice` and `--arch monolithic`? [Coverage, Spec §US1-SC3, US1-SC4]
- [x] CHK-018 Is the edge case specified for when `config.json` has `"agent": "generic"` with no LLM provider mapping — does decompose fall back to `DomainAnalyzer` with a warning? [Edge Case, Spec §US7-SC5]
- [x] CHK-019 Are `--template-mode` and `--dry-run-prompt` flags specified for the decompose command? [Completeness, Spec §FR-031, FR-038]

## 3. Architecture-Specific Content Still Appears Correctly

- [x] CHK-020 Is the `ArchitectureAdapter` protocol extended with a `serialize_for_prompt() -> str` method that replaces Jinja2 template variables with prose prompt text? [Completeness, Spec §FR-010]
- [x] CHK-021 Are all 3 adapter implementations (Microservice, Monolith, ModularMonolith) specified to implement `serialize_for_prompt()`? [Completeness, Plan §Phase 2]
- [x] CHK-022 Is it specified that microservice prompts include Docker, health checks, circuit breakers, gRPC/REST communication, and service discovery? [Coverage, Spec §US3-SC1]
- [x] CHK-023 Is it specified that monolith prompts include shared DB setup, module boundary rules, and shared middleware—with NO mention of containers or service mesh? [Coverage, Spec §US3-SC2]
- [x] CHK-024 Is it specified that modular-monolith prompts include module boundary enforcement AND interface contract definitions? [Coverage, Spec §US3-SC3]
- [x] CHK-025 Is the success criterion for architecture-specific sections in generated artifacts measurable by section heading presence? [Measurability, Spec §SC-002]
- [x] CHK-026 Is there a negative success criterion that monolith output must NOT contain microservice-specific terms (Docker, gRPC, service mesh)? [Measurability, Spec §SC-003]
- [x] CHK-027 Is architecture context serialized as prompt text rather than Jinja2 template variables in every prompt? [Consistency, Spec §FR-010, US3-SC4]

## 4. Token Limits Respected

- [x] CHK-028 Is the default token budget specified as `CONTEXT_TOKEN_BUDGET` (100,000 tokens) using `CHARS_PER_TOKEN_ESTIMATE = 4` for measurement? [Clarity, Spec §FR-013]
- [x] CHK-029 Is the priority-based trimming order explicitly defined: phase instructions (never trimmed) > current service artifacts > prior phase artifacts (newest first) > governance > constitution? [Completeness, Spec §FR-014]
- [x] CHK-030 Is the truncation behavior specified: remaining character budget + `[TRUNCATED]` marker with full document path? [Clarity, Spec §FR-015]
- [x] CHK-031 Is the token budget overridable via `"token_budget"` in `.specforge/config.json`? [Completeness, Spec §FR-016]
- [x] CHK-032 Is there a measurable success criterion that prompt character count stays within `token_budget * CHARS_PER_TOKEN_ESTIMATE`? [Measurability, Spec §SC-008]
- [x] CHK-033 Is the `max_output_chars` cap (default: 200,000 characters) specified for combined output from continuation calls? [Completeness, Spec §FR-042]
- [x] CHK-034 Is the continuation mechanism specified: up to 3 continuation calls per phase, detecting truncation via missing required sections + incomplete trailing content? [Completeness, Spec §FR-040, FR-041]
- [x] CHK-035 Is the edge case of oversized LLM output (50,000+ words) addressed with a configurable maximum output length? [Edge Case, Spec §Edge Cases]
- [x] CHK-036 Are governance prompts filtered per-phase via `GOVERNANCE_PHASE_MAP` to reduce token consumption? [Completeness, Spec §FR-009, FR-043]
- [x] CHK-037 Is the `GOVERNANCE_PHASE_MAP` default mapping explicitly defined for all 8 phases using real `GOVERNANCE_DOMAINS` names (e.g., datamodel → [database, backend, security]; edgecase → [security, testing, backend]; decompose → all domains)? [Clarity, Spec §FR-043]

## 5. Templates Only Used in Init (No Regression)

- [x] CHK-038 Is it explicitly stated that `specforge init` scaffolding continues to use Jinja2 templates exclusively and is NOT modified by this feature? [Completeness, Spec §FR-034]
- [x] CHK-039 Is it stated that `TemplateRenderer`, `TemplateRegistry`, and all `.md.j2` template files remain in the codebase and fully functional? [Completeness, Spec §FR-033]
- [x] CHK-040 Is the `--template-mode` flag specified for both `specforge decompose` and `specforge specify`? [Completeness, Spec §FR-031]
- [x] CHK-041 Is auto-fallback to template mode specified when no LLM provider is available? [Completeness, Spec §FR-032]
- [x] CHK-042 Is there a success criterion that `--template-mode` produces byte-identical output to the pre-Feature-015 pipeline? [Measurability, Spec §SC-004]
- [x] CHK-043 Is the fallback timing specified (within 10 seconds including timeout + detection)? [Clarity, Spec §SC-005]
- [x] CHK-044 Is it specified that `--template-mode` and `--dry-run-prompt` are mutually exclusive with a clear CLI rejection error? [Consistency, Spec §FR-039]
- [x] CHK-045 Is the edge case addressed for when `config.json` exists but has no `"agent"` field — defaulting to template mode with a warning? [Edge Case, Spec §Edge Cases]
- [x] CHK-046 Is it clear that template mode produces NO behavioral changes relative to pre-Feature-015 for decompose as well? [Completeness, Spec §US4-SC4]

## 6. Output Is Clean Markdown Only

- [x] CHK-047 Is a clean-markdown-only instruction specified for all phase system prompts: "Output ONLY the Markdown document. No preamble, commentary, or conversational text"? [Completeness, Spec §FR-030]
- [x] CHK-048 Is the `OutputPostprocessor.strip_preamble()` method specified to strip common LLM preamble patterns before the first markdown heading? [Completeness, Spec §FR-030]
- [x] CHK-049 Is heading-level normalization specified via `OutputPostprocessor.normalize_headings()` for when the LLM returns incorrect heading levels (e.g., `###` instead of `##`)? [Edge Case, Spec §Edge Cases, Data Model §OutputPostprocessor, Tasks §T022/T024]
- [x] CHK-050 Is per-phase output validation specified with required section headings (e.g., spec.md → `## User Scenarios`, `## Requirements`, `## Success Criteria`)? [Completeness, Spec §FR-026, FR-027]
- [x] CHK-051 Is the retry-on-validation-failure mechanism specified: corrective prompt listing missing sections, up to configured retry limit? [Completeness, Spec §FR-028]
- [x] CHK-052 Is the `.draft.md` save behavior specified for when validation fails after all retries? [Completeness, Spec §FR-029]
- [x] CHK-053 Are the required sections for each phase explicitly listed (spec, plan, tasks, checklist, research, data-model, edge-cases)? [Clarity, Spec §FR-024, FR-026]
- [x] CHK-054 Is the `OutputValidator` required-sections list derived from Spec-Kit template skeletons? [Consistency, Spec §FR-046]
- [x] CHK-055 Is the edge case addressed for LLM output containing implementation-specific content (code blocks, framework names) in spec-phase artifacts? [Edge Case, Spec §Edge Cases]
- [x] CHK-056 Are Spec-Kit format conformance criteria measurable — e.g., `FR-NNN` IDs in spec, `T001` checkbox format in tasks, `CHK-001` format in checklist? [Measurability, Spec §SC-011]
- [x] CHK-057 Is the configurable timeout (default 120s) and retry policy (3x exponential backoff: 1s, 2s, 4s) specified for LLM provider calls? [Completeness, Spec §FR-005, FR-006]
- [x] CHK-058 Is the edge case addressed for when the LLM returns empty content or a connection error? [Edge Case, Spec §US6-SC5]

## Notes

- 58 total items across 6 categories, mapped to 48 FRs, 14 SCs, 7 user stories, and 10 edge cases.
- All items test requirements quality (completeness, clarity, consistency, measurability, coverage) — not implementation behavior.
- Traceability: every item references at least one Spec section (`§FR-NNN`, `§SC-NNN`, `§US-SCN`, or `§Edge Cases`) or uses a `[Gap]` / `[Edge Case]` marker.
- Categories align with the user's 6 requested focus areas.
