<!-- Generated by SpecForge — do not edit manually above the Rules section -->

# Backend Governance Prompt ({{ stack_hint }})

## Meta
domain: backend
stack: python
version: 1.0
precedence: 3
checksum: {{ checksum }}

## Precedence
This file occupies position 3 in the conflict-resolution hierarchy:
`security (1) > architecture (2) > backend/frontend/database (3) > testing (4) > cicd (5)`

When this file conflicts with a higher-precedence file, the higher-precedence
value applies. When this file conflicts with an equal-priority file, the conflict
is flagged as AMBIGUOUS and requires manual resolution.

## Rules

### BACK-001: Type Annotations Required
severity: ERROR
scope: all Python function and method signatures
rule: All function parameters and return values MUST have type annotations. Using `Any` without an explicit comment explaining why is prohibited. `from __future__ import annotations` MUST be at the top of every file.
threshold: max_function_lines=30, max_complexity=10
example_correct: |
  def process_order(order: Order) -> Result[ProcessedOrder, str]: ...
example_incorrect: |
  def process_order(order):  # no annotations — prohibited
      return None

### BACK-002: PEP 8 and Ruff Compliance
severity: ERROR
scope: all Python source files
rule: All code MUST pass `ruff check` with zero errors. Line length MUST NOT exceed 88 characters. Imports MUST be sorted using ruff's isort rules. Wildcard imports (`from module import *`) are prohibited.
threshold: max_function_lines=30
example_correct: |
  from pathlib import Path
  from specforge.core.result import Ok, Err
example_incorrect: |
  from specforge.core.result import *  # wildcard — prohibited
