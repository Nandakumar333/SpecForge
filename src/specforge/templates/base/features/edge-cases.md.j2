{% include "base/partials/generation-header.md.j2" %}

{# Guidance: Document edge cases that could cause failures. #}
{# For each case, describe the trigger, expected behavior, and test approach. #}
{# Prioritize cases most likely to occur in production. #}

# Edge Cases — {{ project_name }}

**Feature**: {{ feature_name | default("unnamed-feature") }}
**Created**: {{ date }}
{% if service %}

**Service**: {{ service.name }} (`{{ service.slug }}`)
**Architecture**: {{ architecture }}

## Feature Coverage

{% for feat in features %}
- **{{ feat.display_name }}**: {{ feat.description }}
{% endfor %}
{% if edge_cases is defined and edge_cases %}

## Edge Cases ({{ edge_cases | length }})

{% for ec in edge_cases %}
### {{ ec.id }}: {{ ec.category | replace("_", " ") | title }}

```yaml
id: {{ ec.id }}
category: {{ ec.category }}
severity: {{ ec.severity }}
affected_services:
{% for svc in ec.affected_services %}- {{ svc }}
{% endfor %}handling_strategy: "{{ ec.handling_strategy }}"
test_suggestion: "{{ ec.test_suggestion }}"
```

**Scenario**: {{ ec.scenario }}

**Trigger**: {{ ec.trigger }}

{% endfor %}
{% else %}

## Edge Cases

{% for feat in features %}
### EC-{{ loop.index }}: {{ feat.display_name }} — Input Validation

- **Trigger**: Invalid or missing input for {{ feat.display_name | lower }}
- **Expected Behavior**: Return descriptive error, no state mutation
- **Test Approach**: Unit test with boundary inputs
- **Priority**: High

{% endfor %}
{% if adapter_edge_cases %}

## Architecture-Specific Edge Cases

{% for ec in adapter_edge_cases %}
### EC-A{{ loop.index }}: {{ ec.name }}

- **Trigger**: {{ ec.description }}
- **Expected Behavior**: Graceful degradation with appropriate error handling
- **Test Approach**: Integration test simulating failure scenario
- **Priority**: High

{% endfor %}
{% endif %}
{% endif %}
{% else %}

## Edge Cases

### EC-001: [Title]

- **Trigger**: _What causes this edge case?_
- **Expected Behavior**: _How should the system respond?_
- **Test Approach**: _How to verify correct behavior._
- **Priority**: High / Medium / Low

### EC-002: [Title]

- **Trigger**: _What causes this edge case?_
- **Expected Behavior**: _How should the system respond?_
- **Test Approach**: _How to verify correct behavior._
- **Priority**: High / Medium / Low
{% endif %}

## Boundary Conditions

- _List inputs at the boundaries of valid ranges._
- _Include empty inputs, maximum sizes, and special characters._
